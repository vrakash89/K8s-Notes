Lowest level of deployment is pod in k8s
docker run -d image -p portnumber --network

these configuration add in pod.yaml 

single container or group of containers

side car container/init container --support to ur actual container

if u have group of containers in a single pod in k8s -k8s will allow u shared networking shared storage
in this case container A can talk to each other by localhost -(eg: localhost:3000)

cluster ip ---kube proxy is generated by cluster ip address

minikube start --memory=4096 --driver=hyperkit